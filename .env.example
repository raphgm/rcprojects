# Firebase Config (handled by platform, but documented here)

# Session API (real terminal backend)
# VITE_SESSION_API_URL=ws://localhost:3001

# Hostinger FTP deploy
FTP_HOST=
FTP_USER=
FTP_PASSWORD=
FTP_PORT=21
FTP_REMOTE_DIR=public_html

